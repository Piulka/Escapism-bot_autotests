# QA findings

Живой backlog дефектов и улучшений, найденных при исследовании Escapism и
разработке автотестов.

Статусы: `New`, `Confirmed`, `In progress`, `Fixed`, `Won't fix`.
Приоритеты: `P0` — блокирует критичный путь, `P1` — значимый риск,
`P2` — заметная проблема, `P3` — улучшение.

## API и инфраструктура

### API-001 — Периодические продолжительные ответы 502

- Тип: bug / test environment stability
- Приоритет: P1
- Статус: Confirmed (повторно воспроизведено 2026-08-21)
- Затронуто: `GET /api/v1/profile`, `GET /api/guild`,
  `GET /api/guild/members`, `GET /api/guild/applications`,
  `POST /api/v1/account/reset`, `POST /api/guild/leave`,
  `POST /api/warehouse/withdraw`
- Наблюдение: endpoint может возвращать `502 Bad Gateway` несколько раз подряд
  после успешной UI-операции или во время setup/cleanup. В UI это также
  воспроизводится экраном `Не удалось загрузить данные → Повторить` при открытии
  каталога гильдий сразу после успешного создания.
- Влияние: корректный пользовательский сценарий становится красным из-за
  недоступности setup, проверки или teardown. Один `502` от account reset
  блокирует все зависящие от fixture тесты, а ошибка guild cleanup оставляет
  состояние, влияющее на последующие экраны.
- Автотесты: retry и best-effort cleanup не используются — любой `502` остаётся
  видимым падением с endpoint, статусом и телом ответа.
- Ожидание: найти причину gateway errors и обеспечить стабильный test API.
- Результат расследования: процесс PHP 8.6 падал с `SEGV/ABRT` при сочетании
  TrueAsync и `opcache.jit=tracing`. После `opcache.jit=disable` повторные
  точечные проверки прошли, но полный последовательный E2E-прогон снова получил
  502 на `GET /api/v1/profile`. Retry в тесты не добавлялся.

### API-002 — Account reset не очищает связанные состояния

- Тип: documented limitation / testability improvement
- Приоритет: P2
- Статус: Confirmed
- Затронуто: guild membership, guild bank и mail.
- Наблюдение: актуальный `API_REFERENCE.md` явно исключает guild membership,
  guild bank и mail из reset. Личный warehouse, наоборот, входит в контракт очистки.
- Влияние: тесты вынуждены иметь отдельный cleanup и восстанавливать дельты.
- Дополнительное влияние: после неуспешного guild cleanup обычный account reset
  сохраняет membership. В результате независимый inventory-тест получает другое
  состояние UI (`Переместить` вместо `На склад`) и корректно падает на нарушенной
  предусловии.
- Ожидание: тесты выполняют отдельный cleanup guild/mail; полный test reset был бы
  полезен, но отсутствие этой очистки больше не считается недокументированным bug.

### API-003 — Контракт exchange изменился без актуальной документации

- Тип: documentation defect
- Приоритет: P2
- Статус: Fixed (документация актуализирована 2026-08-21)
- Наблюдение: webapp начал передавать `minAmountOut`, но поле отсутствовало в
  описании `POST /api/exchange`.
- Влияние: контрактный assertion smoke-теста обнаружил незадокументированное
  изменение.
- Проверка фикса: документация описывает `minAmountOut`, минимум 100,
  целочисленную комиссию `ceil(5%)` и реинвестирование `fee // 2`; формулы тестов
  приведены к опубликованному контракту.

### API-004 — Account reset возвращает 500 во время деградации стенда

- Тип: bug / test environment stability
- Приоритет: P1
- Статус: Confirmed (повторно воспроизведено 2026-08-21)
- Наблюдение 2026-08-21: помимо периодических `502`, `POST
  /api/v1/account/reset` несколько раз вернул `500 Internal Server Error` с
  телом `{"error":"Internal Server Error","code":"INTERNAL_ERROR"}`.
- Влияние: fixture не может создать известное начальное состояние; зависимые
  тесты корректно останавливаются на setup.
- Ожидание: reset тестового пользователя должен быть транзакционным и стабильно
  возвращать 200 либо диагностируемую доменную ошибку, а не generic 500.
- Проверка: отдельный контрактный reset-тест прошёл, но в полном последовательном
  прогоне `POST /api/v1/account/reset` снова вернул generic 500. Серверное
  логирование Throwable должно дать точную причину этого запроса.

### API-005 — Account reset очищал почту вопреки прежней документации

- Тип: API contract / documentation defect
- Приоритет: P1
- Статус: Fixed / contract synchronized (проверено 2026-08-22)
- Актуализация: текущий `API_REFERENCE.md` явно требует удалить прежние письма и создать
  ровно 5 seed-писем. Контрактный тест создаёт уникальное письмо, выполняет reset,
  проверяет его удаление и точный набор пяти тем — тест проходит.
- История: прежняя версия документации утверждала, что `mails` не сбрасываются,
  хотя backend уже выполнял `clearMails/seedMails`.
- Итог: текущая документация и реализация согласованы; reset намеренно удаляет
  прежние письма и создаёт пять seed-писем в известных read/claim-состояниях.
- Автотест: проверяет удаление уникального письма и точный набор пяти seed-тем без
  `xfail`, retry и обходов.

## UX/UI и accessibility

### UI-001 — У кнопки фильтров инвентаря нет доступного имени

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Fixed (проверено 2026-08-21)
- Наблюдение: button содержит только `svg.lucide-funnel`, без `aria-label` и
  видимого текста.
- Влияние: screen reader не объясняет действие; тест использует CSS-класс иконки.
- Ожидание: добавить `aria-label="Фильтры"`.
- Проверка фикса: regression-тест использует role/name `Фильтры` без CSS-иконки.

### UI-002 — Equipment slots не имеют стабильных locator

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Fixed (основной accessibility-контракт, проверено 2026-08-21)
- Наблюдение: все 8 equipment slots реализованы как кликабельные `div` без
  `role`, `aria-label` и `data-testid`; smoke-тест временно использует
  `locator("svg").nth(3)` для слота головы.
- Влияние: изменение порядка SVG может сломать тест без изменения функции.
- Ожидание: accessible button name или `data-testid` с ключом equipment slot.
- Проверка фикса: slot получил role, keyboard semantics, aria-label и test id.
  Дублирование test id в responsive DOM вынесено отдельно как UI-016.

### UI-003 — Строка участника гильдии не является доступной кнопкой

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Confirmed
- Наблюдение: управление участником открывается кликом по тексту ника; button role
  и доступное имя отсутствуют.
- Ожидание: использовать `<button>` либо добавить корректные role, keyboard
  interaction и accessible name.

### UI-004 — Карточки выбора почтовых вложений не имеют стабильного имени

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Fixed (проверено по DOM 2026-08-21)
- Наблюдение: элементы временно выбираются по позиции `div.aspect-square`.
- Дополнительное наблюдение: кнопки удаления уже прикреплённых предметов также
  не имеют accessible name и определяются по `svg.lucide-trash2`.
- Влияние: сортировка inventory способна выбрать другое вложение.
- Ожидание: accessible name с названием предмета и количеством либо test id с
  item ID; для удаления — имя вида `Удалить вложение: Морковь`.
- Проверка фикса: карточки доступны как `Прикрепить {name} ({quantity})`, кнопки
  удаления — как `Удалить вложение: {name}`. Тесты переведены на эти имена.

### UI-005 — Unread-индикатор письма не имеет семантического состояния

- Тип: accessibility / testability
- Приоритет: P3
- Статус: Confirmed
- Наблюдение: состояние проверяется по CSS-классу.
- Ожидание: добавить текст для screen reader, `aria-label` или `data-testid` с
  явным read/unread state.

### UI-006 — Кнопка направления обмена не имеет доступного имени

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Fixed (проверено 2026-08-21)
- Наблюдение: locator строится по `svg.lucide-arrow-down-up`.
- Ожидание: динамический `aria-label`, например `Обменять золото на серебро`.
- Проверка фикса: exchange-тесты используют динамические role/name locator'ы.

### UI-007 — Крестик карточки предмета не имеет доступного имени

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Fixed (проверено 2026-08-21)
- Наблюдение: основной item dialog содержит первым элементом button только с
  иконкой закрытия; доступное имя отсутствует.
- Влияние: screen reader не объясняет действие, а тест вынужден выбирать первую
  кнопку внутри dialog.
- Ожидание: добавить `aria-label="Закрыть карточку предмета"`.
- Проверка фикса: dialog закрывается через role/name `Закрыть`.

### UI-008 — Заблокированные действия предмета теряют назначение

- Тип: UX / accessibility
- Приоритет: P2
- Статус: Fixed (проверено автотестом 2026-08-21)
- Наблюдение после частичного фикса 2026-08-21: `На склад — заблокировано` и
  `Разобрать — заблокировано` различимы, `Выбросить предмет` сохраняет назначение,
  но действие экипировки всё ещё называется только `Заблокировано`.
- Влияние: пользователь screen reader не может определить, какое действие было
  запрещено; сообщения теста также теряют контекст.
- Ожидание: сохранить название действия и объяснить причину, например
  `Надеть — недоступно: предмет заблокирован`.
- Проверка фикса: `Надеть — заблокировано` и остальные имена проверяются без
  xfail; сценарий lock/unlock проходит.

### UI-009 — Выброс предмета выполняется без подтверждения

- Тип: UX / destructive action
- Приоритет: P1
- Статус: Fixed (проверено 2026-08-21)
- Наблюдение: нажатие `Выбр.` в карточке предмета сразу отправляет
  `POST /api/inventory/trash`; диалог подтверждения отсутствует. На viewport
  VK Mini App название действия также сокращено до неочевидного `Выбр.`.
- Влияние: пользователь может безвозвратно удалить предмет случайным нажатием и
  не получает возможности проверить выбранный предмет перед операцией.
- Ожидание: показывать подтверждение с названием предмета и явными действиями
  `Отмена` / `Выбросить`; использовать полное доступное имя кнопки даже при
  визуально сокращённой подписи.
- Проверка фикса: confirmation-flow проходит без xfail; диалог содержит имя
  предмета, а `POST /api/inventory/trash` отправляется после подтверждения.

### UI-010 — Информационные контролы биржи не имеют доступных имён

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Fixed (проверено 2026-08-21)
- Наблюдение: кнопка открытия информации определяется только по
  `svg.lucide-info`, а кнопка закрытия внутри `dialog "О Бирже валюты"` не имеет
  accessible name.
- Влияние: назначение кнопок не объявляется screen reader; UI-тест зависит от
  реализации иконок.
- Ожидание: добавить доступные имена `О Бирже валюты` и `Закрыть информацию о
  Бирже`.
- Проверка фикса: оба контрола находятся и используются по role/name.

### UI-011 — После повторной подачи список заявок не согласован с badge

- Тип: functional bug
- Приоритет: P1
- Статус: Fixed (core flow проверен 2026-08-22)
- Актуализация: повторное открытие каталога после reject снова показывает кнопку подачи;
  повторная заявка создаётся, лидер видит её и успешно принимает. Lifecycle дошёл дальше
  прежней точки `xfail`; последующее падение связано с изменившимся UI выхода участника,
  а не с UI-011.
- Причина: оптимистичный `hasApplication=true` оставался в QueryClient кандидата;
  каталог не перечитывался при remount в пределах `staleTime`.
- Итоговая проверка: после reject повторное открытие каталога показывает кнопку
  `Подать заявку`; повторный join создаёт заявку, лидер видит её и успешно принимает.
- Остаточное продуктовое поведение: уже открытая вкладка другого пользователя не
  получает push-сигнал и может быть stale до 30 секунд либо ручного refresh. Это не
  классифицируется как UI-011.

### UI-012 — UI ограничивает текст набора 250 символами вместо 500

- Тип: functional / contract mismatch
- Приоритет: P2
- Статус: Fixed (проверено автотестом 2026-08-21)
- Наблюдение: `API_REFERENCE.md` и backend-контракт допускают текст набора до 500
  символов, но textarea формы имеет `maxlength="250"` и обрезает остаток.
- Влияние: валидный по API текст невозможно ввести через пользовательский UI.
- Ожидание: привести UI к документированному лимиту 500 либо согласованно изменить
  контракт и документацию, если продуктовый лимит должен быть равен 250.
- Проверка фикса: textarea принимает 500 символов, тест проходит без xfail.

### UI-013 — Кнопка редактирования текста набора не имеет доступного имени

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Fixed (проверено автотестом 2026-08-21)
- Наблюдение: button содержит только `svg.lucide-pen`, без `aria-label`, видимого
  текста и test id.
- Влияние: screen reader не сообщает назначение кнопки; тест зависит от класса
  иконки.
- Ожидание: добавить `aria-label="Редактировать текст набора"`.

### UI-014 — Обычный участник видит редактирование текста набора

- Тип: authorization UX / functional bug
- Приоритет: P1
- Статус: Confirmed
- Наблюдение: у пользователя с `myRole="member"` отображается кнопка с
  `svg.lucide-pen`, открывающая форму редактирования публичного текста набора.
  По API-документации операция разрешена только лидеру.
- Влияние: UI предлагает участнику запрещённое действие; фактическая защита
  зависит только от backend.
- Ожидание: не отображать контрол редактирования для member. Backend-проверку
  прав сохранить независимо от UI.
- Проверка фикса: участник не видит кнопку редактирования; прямой API-запрет
  проверяется отдельным тестом.

### UI-015 — Карточка перекрывает кнопку details технологии

- Тип: layout / functional bug
- Приоритет: P1
- Статус: Fixed (проверено автотестом 2026-08-21)
- Наблюдение: на экране технологий кнопка `Детали` видима и enabled, но pointer
  events перехватывает родительский кликабельный контейнер карточки. Обычный click
  не достигает кнопки.
- Влияние: пользователь не может открыть подробности технологии штатным способом.
- Ожидание: исправить слои/обработчики карточки, чтобы вложенная кнопка была
  кликабельна без force-click и всплытие события обрабатывалось корректно.
- Проверка фикса: тест проверяет `aria-expanded`, раскрывает аккордеон и открывает
  details-dialog без force-click и xfail.

### UI-016 — Equipment slot дублирует data-testid в DOM

- Тип: testability / markup quality
- Приоритет: P3
- Статус: Fixed / не воспроизводится на актуальном бандле (2026-08-21)
- Наблюдение: `data-testid="equipment-slot-mainHand"` одновременно находится на
  двух элементах responsive-разметки. Обычный `get_by_test_id` получает strict
  mode violation.
- Проверка: `equipment-slot-mainHand` снова используется как уникальный
  `get_by_test_id`; regression-сценарий проходит без CSS-фильтра `:visible`.
- Ожидание: не монтировать скрытую копию slot либо обеспечить уникальность test id
  между responsive-представлениями.

### UI-017 — Информационные кнопки желудка и мощи не имеют понятных имён

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Confirmed (2026-08-21)
- Наблюдение: кнопки над inventory доступны как `T1 ›` и `0 ›`; назначение
  «Желудок» и «Мощь персонажа» присутствует только в открываемых диалогах.
- Влияние: screen reader объявляет значение без смысла, а тест не может выбрать
  контрол по пользовательскому назначению.
- Ожидание: добавить динамические `aria-label`, например `Желудок, ранг T1` и
  `Мощь персонажа, 0`.
- Временный locator: тест желудка вычисляет `T{level} ›` из API-профиля. После
  исправления UI-017 его нужно заменить на `get_by_role(..., name="Желудок…")`.

### UI-018 — Диалог мощности персонажа не имеет доступного имени

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Confirmed (2026-08-21)
- Наблюдение: контейнер имеет `role="dialog"` и `aria-modal="true"`, но без
  `aria-label`/`aria-labelledby`; заголовок «Мощь персонажа» визуально существует.
- Влияние: screen reader объявляет безымянный диалог, а тест вынужден связывать
  контейнер с вложенным heading через `filter(has=...)`.
- Ожидание: связать dialog с heading через `aria-labelledby` либо добавить
  `aria-label="Мощь персонажа"`.

### UI-019 — Кнопка настройки карточки профиля не имеет доступного имени

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Confirmed (2026-08-21)
- Наблюдение: icon-only button содержит только
  `title="Настройка карточки"`, без `aria-label` и видимого текста.
- Влияние: HTML title полезен для tooltip, но назначение кнопки нестабильно
  объявляется assistive technology; тест временно использует `get_by_title`.
- Ожидание: добавить `aria-label="Настройка карточки"` или эквивалентное
  доступное имя и перевести тест на role/name locator.

### UI-020 — Названия вариантов оформления профиля дублируются в accessibility tree

- Тип: accessibility / testability
- Приоритет: P2
- Статус: Confirmed (2026-08-21)
- Наблюдение: у кнопок выбора фона, рамки и аватара одинаковый текст задан и как `alt`
  вложенного изображения, и как видимая подпись. Например, карточка «Вулкан» получает
  доступное имя `Вулкан Вулкан` вместо `Вулкан`.
- Влияние: screen reader дважды произносит одно название, а семантический locator по
  ожидаемому пользовательскому имени не находит кнопку. Тест временно ограничивает
  `button` по его видимому тексту.
- Ожидание: декоративное изображение внутри уже подписанной кнопки должно иметь пустой
  `alt`, либо сама кнопка должна получить единичный `aria-label`.

### UI-021 — Экипированный шлем не отображается на модели персонажа

- Тип: UI / functional
- Приоритет: P1
- Статус: Not a defect / corrected expectation (проверено 2026-08-22)
- Актуализация: `hideHelmet=true` является продуктовым default. Новый тест явно выключает
  switch и подтверждает появление `avatar-helmet`, затем включает его обратно и
  подтверждает возврат `avatar-body`. Сценарий проходит.
- Уточнение: используется generic `avatar-helmet.webp`, а не индивидуальный спрайт
  конкретного head-item; это текущий продуктовый контракт, не дефект теста.

### UI-022 — Экран состава получает устаревшее состояние после изменения вклада

- Тип: UI / data freshness
- Приоритет: P1
- Статус: Invalid finding / test defect (исправлено 2026-08-22)
- Причина ложного срабатывания: тест сравнивал member-объекты целиком, включая новое
  динамическое поле `lastActivityAt`, которое закономерно меняется при каждом
  авторизованном запросе. Стабильные поля состава и вклада совпадают; UI отображает двух
  участников и вклад `0/1`. Тест разделён на stable business fields и live online fields.
- Итог: `xfail` удалён. Проверка сравнивает стабильные business-поля с API-response,
  отображение вклада и online-status, затем сортировку UI.

### API-006 — GET /api/guild возвращает 500 после reject и повторной заявки

- Тип: API / state consistency
- Приоритет: P1
- Статус: Reclassified as environment stability incident (2026-08-22)
- Актуализация: статической связи с reject/reapply нет; разработчик связал generic 500 с
  прежними падениями процесса/БД. В актуальном прогоне lifecycle прошёл reject, reapply и
  accept без данного 500. Запись сохранена как история нестабильности стенда.
- История: единичный generic 500 ломал обязательный cleanup. Автоматические retry и
  подавление cleanup-ошибок не добавлялись. После исправления инфраструктуры lifecycle
  стабильно проходит эту точку.

### UI-023 — Активная кнопка изучения навыка молча игнорирует недостаток LP

- Тип: UI / validation / UX
- Приоритет: P1
- Статус: Fixed in frontend / verification blocked by API-007 (2026-08-22)
- Исправление разработчика: `disabled={boosting || learningPoints < BOOST_AMOUNT}`.
  Повторная end-to-end проверка пока не достигает UI-assert: успешный `/skills/inject`
  больше не создаёт навык в `GET /v1/profile` (API-007).
- Предусловие исходной находки: API-setup оставлял 9 LP при цене кнопки
  `Учить (10 LP)`.
- Фактически до исправления: кнопка была enabled, но клик не отправлял
  `POST /api/skills/inject` и не показывал пользователю результат.
- Ожидается: disabled-кнопка при `learningPoints < price`; серверная ошибка остаётся
  fallback на случай рассинхрона.

### API-007 — Успешный skills/inject не отражает навык в профиле

- Тип: API / state consistency
- Приоритет: P0 (ломает smoke навыков)
- Статус: Confirmed (2026-08-22)
- Фактически: десять UI-запросов `POST /api/skills/inject` для `spec_sword` возвращают
  200 с корректным payload, но последующий `GET /api/v1/profile` не содержит этот skill.
  То же наблюдается при одном API setup-вливании 441 XP.
- Влияние: падает существующий smoke повышения навыка и заблокирована проверка UI-023.
- Ожидается: успешный inject атомарно сохраняет XP/level и возвращает навык в
  `profile.skills`, либо endpoint отвечает диагностируемой ошибкой вместо ложного 200.

### API-008 — transfer-section возвращает 500 для разрешённого common → officer

- Тип: API / guild storage
- Приоритет: P1
- Статус: Confirmed (2026-08-22)
- Предусловие: лидер и member состоят в одноразовой гильдии; member успешно положил
  `food_raw_1 ×2` в секцию `common`.
- Фактически: `POST /api/guild/storage/transfer-section` с source `common` возвращает
  `500 {"error":"Internal Server Error","code":"INTERNAL_ERROR"}`.
- Ожидается: направление `common → officer` разрешено любому участнику и возвращает 200;
  обратное направление остаётся доступно только officer/deputy/leader.
- Тест не обходит endpoint переносом через сумку.

## Правила добавления findings

- Не записывать предположение как подтверждённый bug.
- Для воспроизводимого дефекта указывать экран, действие, фактический и ожидаемый
  результат.
- Проблемы тестового стенда отделять от продуктовых дефектов.
- После исправления сохранять запись и менять статус на `Fixed` с версией/датой.
